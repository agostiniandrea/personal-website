{
  "buildCommand": "yarn build",
  "framework": "nextjs",
  "installCommand": "corepack enable && corepack prepare yarn@4.9.1 --activate && yarn install",
  "ignoreCommand": "node ./scripts/ignore-vercel.js"
}
