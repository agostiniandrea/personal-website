# ── Contentful (required) ─────────────────────────────────────────────────────
NEXT_PUBLIC_CONTENTFUL_SPACE_ID=""
NEXT_PUBLIC_CONTENTFUL_ACCESS_TOKEN=""

# ── Supabase (required — server-side only, never NEXT_PUBLIC_) ────────────────
SUPABASE_URL=""
SUPABASE_SERVICE_ROLE_KEY=""

# ── Tree-Nation (server-side only, never NEXT_PUBLIC_) ────────────────────────
# Optional: the forest tree counter endpoint is public; the token unlocks the
# authenticated endpoints (projects, species, forest details)
TREE_NATION_API_TOKEN=""

# ── Site ──────────────────────────────────────────────────────────────────────
NEXT_PUBLIC_SITE_URL="https://agostiniandrea.dev"

# ── Analytics (optional) ──────────────────────────────────────────────────────
NEXT_PUBLIC_GA_ID=""
NEXT_PUBLIC_CLARITY_PROJECT_ID=""

# ── Build tools (local only — do not add to Vercel) ───────────────────────────
# Contentful Management API — only needed to run scripts/create-forest-content-type.ts
CONTENTFUL_MANAGEMENT_TOKEN=""
CONTENTFUL_ENVIRONMENT_ID="master"

# Chromatic visual regression — only needed for yarn chromatic
CHROMATIC_PROJECT_TOKEN=""

# Sentry source map upload — only needed if Sentry is re-enabled
# SENTRY_AUTH_TOKEN=""
# SENTRY_ORG=""
# SENTRY_PROJECT=""
