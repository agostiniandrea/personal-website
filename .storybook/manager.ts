import { create } from "@storybook/theming";

const theme = create({
  base: "light",
  brandTitle: "Website Test",
  brandUrl: "https://agostiniandrea.vercel.app/",
});

export default {
  theme,
};
